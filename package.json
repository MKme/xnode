{
  "name": "xnode",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "python support/check_watch_overlay_persistence.py",
    "build": "npm test && pio run -e t-watch-ultra",
    "upload": "pio run -e t-watch-ultra -t upload --upload-port COM10"
  }
}
